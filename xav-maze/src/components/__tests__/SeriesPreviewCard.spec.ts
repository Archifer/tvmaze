import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import SeriesPreviewCard from '../SeriesPreviewCard.vue'

describe('Navbar', () => {
  it('renders properly', () => {
    // const wrapper = mount(SeriesPreviewCard, { props: new Series() { title:"titel", description:"dwd", imageUrl:"", seriesId:"1" } })
    // expect(wrapper.text()).toContain('titel')
  })
})
